@model IEnumerable<DougHomework2.Models.Douglas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Doug.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DougLevel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DougMasteries)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Doug.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DougLevel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DougMasteries)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DouglasId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DouglasId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DouglasId })
        </td>
    </tr>
}

</table>
